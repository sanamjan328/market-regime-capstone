defaults:
  - data: spy_primary
  - model: patchtst_film
  - backtest: default
  - cv: purged_walk_forward
  - _self_

seed: 42
n_seeds: 5
output_dir: reports
track_mlflow: false

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d_%H-%M-%S}
